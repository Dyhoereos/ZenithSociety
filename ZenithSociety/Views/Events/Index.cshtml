@model IEnumerable<ZenithSociety.Models.Zenith.Event>

@{
    ViewBag.Title = "Index";
    var currentDate = new DateTime();
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    @*<tr>
        <th>
            @Html.DisplayNameFor(model => model.Activity.ActivityDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationUser.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EventFrom)
        </th>
        <th>    
            @Html.DisplayNameFor(model => model.EventTo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreationDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th></th>
    </tr>*@

@foreach (var item in Model) {

    if (item.EventFrom.Date != currentDate)
    {
        currentDate = item.EventFrom.Date;

        <tr>
            <th colspan="3">
                @String.Format("{0:dddd, MMMM d, yyyy}", currentDate)       
            </th>
        </tr>
    }
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EventFrom) -
            @Html.DisplayFor(modelItem => item.EventTo)
        </td>
            
        <td>
            @Html.DisplayFor(modelItem => item.Activity.ActivityDesc)
        </td>
        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EventId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EventId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EventId })
        </td>
    </tr>
}
</table>
